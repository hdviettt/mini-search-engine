__pycache__/
*.pyc
.env
*.egg-info/
dist/
build/
.venv/
venv/
node_modules/
.next/
*.db
.DS_Store
ideas/

# .claude/ is committed on purpose — settings.json and skills/ are how this
# repo is maintained. Only machine-specific permissions stay out.
.claude/settings.local.json

# Downloaded cross-encoder weights — fetch with scripts/download_model.py
backend/models/

# Ad-hoc eval reports. The baseline IS committed — a reference point nobody
# else can reproduce is not a reference point.
eval/*-report.json
backup.sql
full_backup.sql
.gstack/
